import React, { useState } from "react";

const Auth = () => {
  // This component is now integrated into Navbar
  // Keeping it for backwards compatibility if needed elsewhere
  return null;
};

export default Auth;
